server:
  port: 80

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/student_db
    username: username
    password: password
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect